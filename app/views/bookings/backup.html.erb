




<div class="booking-widget" style="width: 500px">
  <div class="booking-widget-header">
    <div class='header-caption'>
      <h3> HOTEL NAME CO - needs edit</h3>
        <%= simple_form_for :search, method: "get" do |f| %>

          <div class="form-field"><%= f.input :starts_at, as: :string, required: false, input_html: {class: "datepicker"} %></div>
          <%= f.input :ends_at, as: :string, required: false, input_html: {class: "datepicker"} %>
          <%= f.select :guests, (1..10).to_a %>
          <button type="button", class="btn btn-primary"><%=  link_to "Book Now", bookings_path  %></button>
        <% end %>
      </div>
    </div>
  </div>
</div>



